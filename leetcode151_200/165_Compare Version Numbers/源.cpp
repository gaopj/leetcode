#include<string>
#include<sstream>
using namespace std;

class Solution {
public:
	int compareVersion(string version1, string version2) {
		stringstream ss;
		int index1 = version1.find('.', 0);
		int index2 = version2.find('.', 0);
	

};

int main()
{
	Solution s;
	s.compareVersion("1.0", "1.0");
	return 0;
}